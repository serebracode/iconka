# 📋 INSTRUCTIONS.md - Инструкции для работы над проектом Iconka

> **Для Claude:** Этот файл содержит полный контекст проекта для быстрого восстановления понимания в новом диалоге.

## 🎯 Суть проекта

**Iconka** — Telegram Mini App для просмотра православных икон с возможностью виртуального зажигания свечи. Пользователь выбирает икону из списка, смотрит её в полноэкранном режиме и может "зажечь" виртуальную свечу.

## 📱 Как работает приложение

### Пользовательский сценарий:
1. Пользователь открывает Mini App в Telegram
2. Видит экран загрузки с логотипом (1 секунда)
3. Переходит к списку из 10 православных икон
4. Может искать икону по названию
5. Нажимает на икону → открывается детальный экран
6. Видит икону в полноэкранном режиме
7. Нажимает кнопку (i) → зажигается виртуальная свеча
8. Скроллит вниз → читает информацию об иконе
9. Нажимает кнопку (x) → гасит свечу
10. Кнопка ← возвращает к списку

## 🏗 Техническая архитектура

### Тип приложения: Single Page Application (SPA)
- Один HTML файл
- 3 экрана управляются через CSS классы `.active`
- Навигация без перезагрузки страницы

### Файловая структура:

```
ОБЯЗАТЕЛЬНЫЕ файлы:
├── index.html              # Всё приложение в одном файле
├── data/icons.json         # JSON база данных икон

НЕОБХОДИМЫЕ папки с ресурсами:
├── icons/                  # 10 JPG изображений икон
├── id/                     # 3 SVG файла (иконка, логотип, заглушка)
└── video/                  # 1 PNG файл (изображение свечи)
```

## 🔑 Ключевые технические решения

### 1. Двухуровневая загрузка данных (КРИТИЧЕСКИ ВАЖНО!)

```javascript
// Уровень 1: Попытка загрузить JSON
const response = await fetch('./data/icons.json');
ICONS_DATA = data.icons;

// Уровень 2: Fallback если JSON не загрузился
ICONS_DATA = [ /* все 10 икон */ ];
```

**ВАЖНО:** В fallback должны быть ВСЕ 10 икон, а не только одна!

### 2. Многослойная композиция для свечи

```
Слой 3 (z-index: 20) → Векторное свечение (CSS radial-gradient)
Слой 2 (z-index: 10) → PNG свеча с прозрачностью
Слой 1 (z-index: 1)  → Изображение иконы
```

Все слои по умолчанию скрыты (`opacity: 0`). При нажатии (i) → `opacity: 1`.

### 3. Автоматическая заглушка для изображений

```html
<img src="./icons/vladimirskaya.jpg" 
     onerror="this.src='./id/iconka_noicon.svg'">
```

Если изображение не найдено → автоматически загружается SVG заглушка.

### 4. Telegram Mini App интеграция

```javascript
const tg = window.Telegram?.WebApp;
tg.ready();                    // Инициализация
tg.expand();                   // Полноэкранный режим
tg.HapticFeedback.impactOccurred('light');  // Вибрация
```

## 📊 Данные икон

### Формат JSON (data/icons.json):

```json
{
  "icons": [
    {
      "id": 1,
      "title": "Название иконы",
      "image": "./icons/filename.jpg",
      "description": "Полное описание...",
      "feast_day": "Даты празднования",
      "patronage": "Покровительство"
    }
  ]
}
```

### Текущие 10 икон:

1. Владимирская икона Божией Матери
2. Казанская икона Божией Матери
3. Иверская икона Божией Матери
4. Смоленская икона Божией Матери
5. Знамение (Новгородская) икона Божией Матери
6. Троица Андрея Рублёва
7. Спас Нерукотворный
8. Тихвинская икона Божией Матери
9. Донская икона Божией Матери
10. Почаевская икона Божией Матери

## 🐛 История проблем и решений

### Проблема #1 (РЕШЕНА 21.11.2025)
**Симптом:** В списке отображается только 1 икона вместо 10
**Причина:** Fallback данные содержали только одну икону
**Решение:** Обновлены fallback данные в index.html — добавлены все 10 икон

### Типичные ошибки при разработке:

❌ **Ошибка:** Забыть обновить fallback при добавлении новой иконы
✅ **Правильно:** Всегда обновлять и JSON, и fallback данные

❌ **Ошибка:** Использовать абсолютные пути (`/icons/...`)
✅ **Правильно:** Использовать относительные пути (`./icons/...`)

❌ **Ошибка:** Не проверять работу без JSON файла
✅ **Правильно:** Тестировать fallback механизм

## 🛠 Частые задачи и как их решать

### Задача: Добавить новую икону

**Шаги:**
1. Добавить JPG в папку `icons/`
2. Добавить запись в `data/icons.json`
3. Добавить ту же запись в fallback данные в `index.html` (строка ~258)
4. Протестировать

### Задача: Изменить дизайн свечи

**Где искать:**
- PNG изображение: `video/3.png`
- Свечение (CSS): `#glowLayer` в index.html (строка ~178)
- Анимация: `@keyframes glow` (строка ~38)

### Задача: Изменить время загрузочного экрана

**Где искать:**
```javascript
setTimeout(showListScreen, 1000);  // 1000 = 1 секунда
```

### Задача: Добавить новый экран

**Паттерн:**
1. Добавить `<div id="newScreen" class="screen">...</div>`
2. Создать функцию показа экрана
3. Управлять через `.classList.add('active')`

## 🧪 Тестирование

### Чек-лист для проверки:

- [ ] Отображаются все 10 икон в списке
- [ ] Поиск фильтрует иконы
- [ ] Кнопка X очищает поиск
- [ ] Клик на икону открывает детальный экран
- [ ] Кнопка (i) показывает свечу
- [ ] Кнопка (x) скрывает свечу
- [ ] Кнопка ← возвращает к списку
- [ ] Скролл показывает информацию об иконе
- [ ] Заглушка работает для отсутствующих изображений
- [ ] Fallback данные работают без JSON файла

### Тестирование fallback:

```bash
# Временно переименовать JSON чтобы проверить fallback
mv data/icons.json data/icons.json.backup
# Открыть index.html → должны показаться все 10 икон
mv data/icons.json.backup data/icons.json
```

## 💡 Best Practices для этого проекта

1. **Всегда синхронизировать JSON и fallback данные**
   - Если обновил JSON → обнови fallback
   
2. **Использовать относительные пути**
   - `./icons/...` вместо `/icons/...`
   
3. **Проверять консоль браузера**
   - F12 → Console → смотреть ошибки
   
4. **Тестировать на мобильных устройствах**
   - Chrome DevTools → Device Toolbar
   
5. **Проверять работу без интернета**
   - Fallback должен работать offline

## 🚀 Deployment чек-лист

- [ ] Все изображения загружены в папку `icons/`
- [ ] SVG файлы в папке `id/`
- [ ] PNG свечи в папке `video/`
- [ ] JSON файл в папке `data/`
- [ ] Проверена работа fallback
- [ ] Протестировано на мобильном
- [ ] Загружено на GitHub
- [ ] GitHub Pages включён
- [ ] URL настроен в Telegram Bot

## 🔗 Важные ссылки

- **Рабочий прототип:** https://serebracode.github.io/iconka/
- **Telegram WebApp API:** https://core.telegram.org/bots/webapps
- **GitHub репозиторий:** serebracode/iconka

## 📝 Шаблон для новой иконы

Для быстрого добавления новой иконы используй этот шаблон:

```json
{
  "id": 11,
  "title": "[НАЗВАНИЕ ИКОНЫ]",
  "image": "./icons/[FILENAME].jpg",
  "description": "[ИСТОРИЯ И ОПИСАНИЕ ИКОНЫ]",
  "feast_day": "[ДАТЫ ПРАЗДНОВАНИЯ]",
  "patronage": "[КОМУ/ЧЕМУ ПОКРОВИТЕЛЬСТВУЕТ]"
}
```

## 🎨 CSS переменные Telegram темы

```css
/* Автоматически подстраиваются под тему пользователя */
--tg-theme-bg-color              /* Основной фон */
--tg-theme-text-color            /* Основной текст */
--tg-theme-hint-color            /* Подсказки, вторичный текст */
--tg-theme-link-color            /* Ссылки */
--tg-theme-button-color          /* Кнопки */
--tg-theme-button-text-color     /* Текст на кнопках */
--tg-theme-secondary-bg-color    /* Вторичный фон */
```

## ⚠️ КРИТИЧЕСКИ ВАЖНО ПОМНИТЬ

1. **Fallback данные должны содержать ВСЕ иконы** — это самая частая ошибка
2. **Paths должны быть относительными** (`./` а не `/`)
3. **Всегда тестировать без JSON** чтобы убедиться что fallback работает
4. **Синхронизировать JSON и fallback** при изменениях

## 🤖 Для Claude при новом диалоге

Если ты (Claude) читаешь это в новом диалоге:

1. **Прочитай сначала README.md** — там общее описание проекта
2. **Потом этот файл** — здесь технические детали
3. **Попроси показать index.html** если нужно увидеть код
4. **Проверь data/icons.json** чтобы увидеть текущие данные

**Главное что нужно знать:**
- Это SPA с 3 экранами
- Двухуровневая загрузка (JSON + fallback)
- 10 икон + виртуальная свеча
- Telegram Mini App интеграция

---

**Дата создания:** 21 ноября 2025  
**Версия проекта:** 1.0.1  
**Автор документации:** Claude (Anthropic)
