# 🕯 Iconka - Telegram Mini App для православных икон

## 📖 О проекте

**Iconka** — это Telegram Mini App для просмотра православных икон с возможностью виртуального зажигания свечей. Приложение создано для духовного использования в Telegram.

### Основные возможности:
- 📱 Просмотр каталога из 10 православных икон
- 🔍 Поиск икон по названию
- 🕯️ Виртуальное зажигание свечи перед иконой
- 📝 Подробная информация о каждой иконе (описание, дни памяти, покровительство)
- 🎨 Адаптация под темную/светлую тему Telegram
- 📲 Полная интеграция с Telegram Mini App API

## 🛠 Технологический стек

- **Frontend:** Vanilla JavaScript (ES6+), HTML5, CSS3
- **Platform:** Telegram Mini App (Telegram WebApp SDK)
- **Hosting:** GitHub Pages (рекомендуется)
- **Data Format:** JSON
- **Design:** Адаптивный дизайн в стиле iOS/Material Design

## 📂 Структура проекта

```
iconka/
│
├── 📄 index.html                    # Главный файл приложения (SPA)
│
├── 📁 data/
│   └── 📄 icons.json               # База данных икон (JSON)
│
├── 📁 icons/                        # Изображения икон
│   ├── vladimirskaya.jpg           # Владимирская икона
│   ├── kazanskaya.jpg              # Казанская икона
│   ├── iverskaya.jpg               # Иверская икона
│   ├── smolenskaya.jpg             # Смоленская икона
│   ├── znameniye.jpg               # Знамение (Новгородская)
│   ├── troica_rublev.jpg           # Троица Андрея Рублёва
│   ├── spas_nerukotvorny.jpg       # Спас Нерукотворный
│   ├── tikhvinskaya.jpg            # Тихвинская икона
│   ├── donskaya.jpg                # Донская икона
│   └── pochaevskaya.jpg            # Почаевская икона
│
├── 📁 id/                           # UI элементы (SVG)
│   ├── iconka_icon.svg             # Иконка приложения (107×107px)
│   ├── iconka_logotype.svg         # Логотип (183×27px)
│   └── iconka_noicon.svg           # Заглушка для отсутствующих изображений
│
├── 📁 video/                        # Анимация и эффекты
│   └── 3.png                       # Изображение свечи (PNG с альфа-каналом)
│
├── 📄 README.md                    # Этот файл
└── 📄 INSTRUCTIONS.md              # Инструкции для разработки
```

## 🎯 Архитектура приложения

### Экраны (Single Page Application):

1. **Loading Screen** (`#loadingScreen`)
   - Показывается 1 секунду при запуске
   - Отображает логотип и иконку приложения
   - Автоматический переход к списку

2. **List Screen** (`#listScreen`)
   - Список всех икон с превью
   - Поиск по названию
   - Сортировка по алфавиту

3. **Detail Screen** (`#detailScreen`)
   - Полноэкранное изображение иконы
   - Кнопка зажигания свечи (i) → (x)
   - Информация об иконе (скролл вниз)
   - Анимация свечения

### Ключевые компоненты:

```javascript
// Глобальные переменные
let ICONS_DATA = [];          // Массив икон (из JSON или fallback)
let selectedIcon = null;      // Текущая выбранная икона
let candleVisible = false;    // Состояние свечи (вкл/выкл)

// Основные функции
loadIconsData()              // Загрузка JSON с fallback
renderIconsList()            // Рендер списка икон
selectIcon(id)               // Открытие детального экрана
toggleCandle()               // Включение/выключение свечи
```

## 🔄 Механизм загрузки данных

### Двухуровневая система с Fallback:

```javascript
async function loadIconsData() {
    try {
        // Уровень 1: Попытка загрузить из JSON
        const response = await fetch('./data/icons.json');
        const data = await response.json();
        ICONS_DATA = data.icons;
    } catch (error) {
        // Уровень 2: Fallback - встроенные данные
        ICONS_DATA = [ /* все 10 икон */ ];
    }
}
```

**Почему это важно:**
- Если JSON файл не найден → используются fallback данные
- Приложение **всегда работает**, даже без внешних файлов
- Все 10 икон доступны в любом случае

## 🎨 Система слоёв (Detail Screen)

При открытии иконы используется многослойная композиция:

```
z-index: 20  → 🔥 Векторное свечение (radial-gradient)
z-index: 10  → 🕯️ PNG свеча с альфа-каналом
z-index: 1   → 🖼️ Изображение иконы (базовый слой)
```

**Анимации:**
- Свеча появляется с fade-in (opacity: 0 → 1)
- Свечение пульсирует (animation: glow 6s infinite)
- Haptic feedback при переключении

## 📱 Telegram Mini App интеграция

```javascript
const tg = window.Telegram?.WebApp;

// Инициализация
tg.ready();                          // Сообщить Telegram что готовы
tg.expand();                         // Развернуть на весь экран
tg.setHeaderColor('#000000');        // Цвет хедера
tg.setBackgroundColor('#000000');    // Цвет фона

// Haptic Feedback
tg.HapticFeedback.impactOccurred('light');   // Лёгкая вибрация
tg.HapticFeedback.impactOccurred('medium');  // Средняя вибрация

// CSS переменные темы
var(--tg-theme-bg-color)            // Цвет фона
var(--tg-theme-text-color)          // Цвет текста
var(--tg-theme-hint-color)          // Цвет подсказок
var(--tg-theme-secondary-bg-color)  // Вторичный фон
```

## 🔍 Структура JSON данных

```json
{
  "icons": [
    {
      "id": 1,
      "title": "Название иконы",
      "image": "./icons/filename.jpg",
      "description": "Полное описание иконы...",
      "feast_day": "Даты празднования",
      "patronage": "Кому/чему покровительствует"
    }
  ]
}
```

## 🚀 Deployment

### GitHub Pages (рекомендуется):

1. Создать репозиторий на GitHub
2. Загрузить все файлы
3. Settings → Pages → Source: main branch
4. URL будет: `https://username.github.io/repo-name/`

### Подключение к Telegram Bot:

1. Создать бота через @BotFather
2. Получить токен
3. Настроить Mini App: `/setmenubutton`
4. Указать URL с GitHub Pages

## ⚠️ Важные технические детали

### 1. Заглушка для изображений
```html
<img src="./icons/icon.jpg" 
     onerror="this.src='./id/iconka_noicon.svg'">
```

### 2. Safe Area для iPhone
```css
.safe-area-top {
    padding-top: env(safe-area-inset-top, 0);
}
.safe-area-bottom {
    padding-bottom: env(safe-area-inset-bottom, 0);
}
```

### 3. Viewport для Telegram
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, 
      maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
```

### 4. Sticky позиционирование
```css
#listHeader {
    position: sticky;
    top: 0;
    z-index: 50;
}
```

## 📝 Как добавить новую икону

1. **Добавить изображение:**
   ```
   icons/novaya_ikona.jpg
   ```

2. **Обновить JSON:**
   ```json
   {
     "id": 11,
     "title": "Новая икона",
     "image": "./icons/novaya_ikona.jpg",
     "description": "Описание...",
     "feast_day": "Дата",
     "patronage": "Покровительство"
   }
   ```

3. **Обновить fallback в index.html:**
   - Найти: `// Fallback - все иконы`
   - Добавить новую икону в массив `ICONS_DATA`

## 🐛 Известные проблемы и решения

### Проблема: Показывается только 1 икона
**Решение:** Проверить fallback данные в index.html (должно быть 10 икон)

### Проблема: Не загружаются изображения
**Решение:** Проверить пути, убедиться что папка `icons/` существует

### Проблема: JSON не загружается
**Решение:** Это нормально! Fallback данные сработают автоматически

## 📊 Текущие данные

- **Всего икон:** 10
- **Размер index.html:** ~27KB
- **Размер icons.json:** ~3KB
- **Поддержка браузеров:** Chrome 90+, Safari 14+, Firefox 88+

## 🔐 Лицензия и использование

- Изображения икон предоставлены для некоммерческого использования
- Все права на изображения принадлежат соответствующим авторам
- Код приложения можно использовать свободно

## 👥 Контакты и поддержка

- **Прототип:** https://serebracode.github.io/iconka/
- **GitHub:** serebracode/iconka

---

**Последнее обновление:** 21 ноября 2025
**Версия:** 1.0.1 (исправлен fallback для всех 10 икон)
